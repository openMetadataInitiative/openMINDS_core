{
  "_type": "core:RasterImage",
  "required": [
    "coordinateFramework",
    "dataLocation",
    "dimension",
    "pixelSize"
  ],
  "properties": {
    "additionalRemarks": {
      "_instruction": "Enter any additional remarks concerning this raster image.",
      "type": "string"
    },
    "capturedWith": {
      "_instruction": "Add the device used to capture this raster image.",
      "_linkedCategories": [
        "deviceUsage"
      ]
    },
    "coordinateFramework": {
      "_instruction": "Add the coordinate space in which this raster image exists.",
      "_linkedTypes": [
        "sands:CommonCoordinateFrameworkVersion",
        "sands:CustomCoordinateFramework"

      ]
    },
    "dataLocation": {
      "_instruction": "Add the location of the file or file bundle in which the raster image is stored.",
      "_linkedTypes": [
        "core:File"
      ]
    },
    "dimension": {
      "_instruction": "Enter the dimension of this raster image in pixels.",
      "items": {
        "type": "integer"
      },
      "maxItems": 2,
      "minItems": 2,
      "type": "array"
    },
    "name": {
      "_instruction": "Enter a descriptive name of this raster image preferably matching the filename.",
      "type": "string"
    },
    "pixelSize": {
      "_embeddedTypes": [
        "core:QuantitativeValue"
      ],
      "_instruction": "Enter the physical pixel size for this raster image (in x,y order).",
      "maxItems": 2,
      "minItems": 2,
      "type": "array"
    }
  }
}

