{
  "_categories": [
    "setupComponent"
  ],
  "required": [
    "contribution",
    "name",
    "type"
  ],
  "properties": {
    "description": {
      "type": "string",
      "_instruction": "Enter a short text describing this device."
    },
    "contribution": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "_instruction": "Add all relevant contributions (e.g., construction, ownership, maintenance) for this device.",
      "_linkedCategories": [
        "legalPerson"
      ]
    },
    "internalIdentifier": {
      "type": "string",
      "_instruction": "Enter the internal identifier for this device."
    },
    "name": {
      "type": "string",
      "_instruction": "Enter a descriptive name for this device, preferably including the model name as defined by the manufacturer."
    },
    "owner": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "_instruction": "Add all parties that legally own this device.",
      "_linkedCategories": [
        "legalPerson"
      ]
    },
    "serialNumber": {
      "type": "string",
      "_instruction": "Enter the serial number of this device."
    },
    "type": {
      "_instruction": "Add the type of this device.",
      "_linkedTypes": [
        "controlledTerms:DeviceType",
        "core:HardwareProduct"
      ]
    }
  }
}
